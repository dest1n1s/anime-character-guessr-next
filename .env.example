# Redis connection URL
# Format: redis://[username:password@]host[:port][/db-number]
# For local development
REDIS_URL=redis://localhost:6379
# For Docker development/production (matches service name in docker-compose)
# REDIS_URL=redis://redis:6379

# Enable Redis in development mode
USE_REDIS_IN_DEV=true

# Application port (used by the application)
PORT=3000 